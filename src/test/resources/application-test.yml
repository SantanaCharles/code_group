spring:
  datasource:
    url: jdbc:h2:mem:portfolio-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    open-in-view: false

app:
  security:
    username: admin
    password: admin123
  membros:
    api:
      base-url: http://localhost:8080/api/external/membros
